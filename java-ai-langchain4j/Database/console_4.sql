CREATE TABLE appointment (
    username varchar(50),
    id_card varchar(50),
    department varchar(50),
    `date` varchar(50),
    `time` varchar(50),
    doctor_name varchar(50)
);
drop table appointment;